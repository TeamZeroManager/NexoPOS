/** RoleRepository (interfaz conceptual) — ver ProductRepository.js para el patrón. */
export class RoleRepository {
  async findAll() {
    throw new Error('RoleRepository.findAll no implementado');
  }
  async findById(id) {
    throw new Error('RoleRepository.findById no implementado');
  }
  async save(role) {
    throw new Error('RoleRepository.save no implementado');
  }
}
